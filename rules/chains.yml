http:
  middlewares:
    secure-chain:
      chain:
        middlewares:
          - secure-headers
          - basic-auth
          - rate-limit
    no-auth-chain:
      chain:
        middlewares:
          - secure-headers
          - rate-limit